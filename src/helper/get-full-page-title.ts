/**
 * Generates a full page title containing "` | Local Library ++`" at the end.
 *
 * @param title - The title of the current page.
 */
export const getFullPageTitle = (title: string): string =>
  title + " | Local Library ++";
